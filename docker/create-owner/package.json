{
  "name": "barray-create-owner-runtime",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone runtime manifest for the `create-owner` Docker image — only what scripts/create-owner.js actually requires (pg, bcryptjs + Node built-ins). Deliberately not part of the root npm workspace or the dashboard runtime image, matching docker/migrate/package.json's pattern (see docker/migrate.Dockerfile).",
  "dependencies": {
    "pg": "^8.13.1",
    "bcryptjs": "^2.4.3"
  }
}
